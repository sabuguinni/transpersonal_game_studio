# Performance Report — PROD_CYCLE_AUTO_20260619_001
**Agent #04 — Performance Optimizer**
**Date:** 2026-06-19

---

## Execution Summary

| Step | Tool | Result |
|------|------|--------|
| 1 | `ue5_execute` bridge validation | `bridge_ok` ✅ |
| 2 | `ue5_execute` CAP enforcement | Actor/dino/light/landscape/playerstart/navmesh audited — `CAP_SAFE:True` ✅ |
| 3 | `ue5_execute` stat commands + CVars | 5 stat commands sent; 16 performance CVars applied ✅ |
| 4 | `ue5_execute` LOD + draw call audit | Static mesh LOD counts + material sections audited; draw call budget checked ✅ |
| 5 | `ue5_execute` NavMesh + tick + save | NavMesh rebuild triggered; prop tick rates set to 10Hz; `MAP_SAVED:True` ✅ |

---

## Performance CVars Applied

### Shadow Quality
| CVar | Value | Rationale |
|------|-------|-----------|
| `r.Shadow.MaxResolution` | 1024 | Balanced quality/cost for prehistoric environment |
| `r.Shadow.RadiusThreshold` | 0.05 | Cull small shadow casters early |
| `r.Shadow.DistanceScale` | 0.6 | Reduce shadow draw distance — outdoor scene has Lumen GI |

### Lumen Global Illumination
| CVar | Value | Rationale |
|------|-------|-----------|
| `r.Lumen.Reflections.Allow` | 1 | Enable Lumen reflections (water, wet rocks) |
| `r.Lumen.HardwareRayTracing` | 0 | Software Lumen — broader GPU compatibility |
| `r.Lumen.TraceMeshSDFs` | 1 | SDF tracing for accurate GI |
| `r.Lumen.GlobalIllumination.MaxTraceDistance` | 8000 | Matches MinPlayableMap scale |

### LOD & Streaming
| CVar | Value | Rationale |
|------|-------|-----------|
| `r.StaticMeshLODDistanceScale` | 1.0 | Default — adjust if draw calls spike |
| `r.SkeletalMeshLODBias` | 0 | Full quality on dino skeletons |
| `r.Streaming.PoolSize` | 1024 | 1GB texture streaming pool |
| `r.Streaming.MaxTempMemoryAllowed` | 50 | Cap temp streaming memory |

### GC & Misc
| CVar | Value | Rationale |
|------|-------|-----------|
| `gc.TimeBetweenPurgingPendingKillObjects` | 30 | Reduce GC hitches |
| `r.MaxAnisotropy` | 4 | Balanced texture filtering |
| `r.AmbientOcclusionLevels` | 1 | Single AO pass — sufficient for outdoor |
| `r.VolumetricFog` | 1 | Keep volumetric fog for atmosphere |

---

## LOD Audit Results

- **Static mesh actors with mesh assigned:** audited
- **Draw call budget target:** < 1000 for MinPlayableMap
- **Skeletal mesh actors:** audited (dinosaur placeholders)
- **LOD chain status:** UE5 engine default LODs active

### Recommendations for Agent #06 (Environment Artist)
- All new static mesh props MUST have at minimum 3 LOD levels (LOD0/LOD1/LOD2)
- LOD0 → LOD1 transition at 15m, LOD1 → LOD2 at 40m, LOD2 → cull at 100m
- Material count per mesh: MAX 3 material sections to keep draw calls low
- Foliage actors: use Hierarchical Instanced Static Mesh (HISM) — NOT individual actors

---

## Tick Rate Optimization

- **Non-player prop actors** (rocks, trees, bushes, grass): tick interval set to **0.1s (10Hz)**
- **Dino AI actors**: left at default tick (managed by Agent #12 Behavior Trees)
- **Player character**: default tick (required for responsive input)

### CPU Budget Targets
| System | Target tick rate | Budget |
|--------|-----------------|--------|
| Player character | 60Hz | 2ms |
| Dino AI (active) | 30Hz | 3ms |
| Dino AI (distant >50m) | 10Hz | 1ms |
| Static props | 10Hz | 0.5ms |
| World systems (weather, day/night) | 5Hz | 0.5ms |

---

## NavMesh Status

- NavMesh rebuild triggered via `r.RebuildNavigation 1`
- `NavMeshBoundsVolume` presence verified in previous cycle
- Agent #12 (Combat AI) should verify NavMesh coverage after placing dino spawn zones

---

## Frame Budget (60fps Target = 16.67ms)

| Category | Budget | Notes |
|----------|--------|-------|
| GPU Render | 8ms | Lumen GI + shadows + fog |
| CPU Game Thread | 4ms | Character + AI + physics |
| CPU Render Thread | 3ms | Draw call submission |
| Headroom | 1.67ms | Buffer for spikes |

---

## Handoff to Agent #05 (Procedural World Generator)

**Performance constraints for PCG/World Partition:**
1. **Actor budget:** MAX 8000 actors in MinPlayableMap at any time
2. **Foliage:** Use HISM components — NOT spawned actors — for trees/grass/rocks
3. **World Partition cell size:** 128m cells recommended for MinPlayableMap scale
4. **Streaming:** Enable distance-based streaming for cells > 200m from player
5. **PCG graph complexity:** MAX 50 PCG nodes per graph — split into sub-graphs if needed

---

## Next Cycle Priorities (Agent #04)

1. Measure actual frame time via `stat unit` output parsing
2. Audit Lumen scene complexity (probe count, mesh SDF coverage)
3. Set up LOD bias on dinosaur placeholder meshes
4. Verify HISM usage for any foliage placed by Agent #06
5. Profile memory usage — target < 4GB VRAM for mid-range GPU compatibility
