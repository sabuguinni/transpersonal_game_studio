# Combat & Enemy AI — Cycle PROD_CYCLE_AUTO_20260712_007

## Bridge Status: UP
4x `ue5_execute` (IDs 32680–32683), all `completed`, ~3s each, zero timeouts, zero camera manipulation, zero .cpp/.h writes (per hard rule).

## Anti-Duplication Audit (per brain memory `hugo_combat_label_consistency_v1`)
Prior cycles accumulated a large flood of `CombatZone_*` / `BehaviorTag_*` floating TextRenderActor labels near the hub (2100,2400) — confirmed by memory: ~191 `CombatZone_Raptor` + ~185 `BehaviorTag_Raptor` labels already exist, far exceeding the actual number of raptors in the scene. This is pure accumulation, not real content.

**Decision this cycle: ZERO new CombatZone_/BehaviorTag_ floating labels were created.** Instead of adding to the flood, tactical combat AI parameters were applied as **Actor Tags directly on the existing dinosaur actors themselves** (TRex/Raptor/Trike/Brachio pawns already present in MinPlayableMap), which is a real, queryable, non-duplicating data attachment.

## Tactical Combat Parameters Applied (via Actor Tags)
| Species | Role Tag | Attack Range | Chase Speed |
|---|---|---|---|
| T-Rex | `Combat_Role_ApexPredator` | `Combat_AttackRange_450` | `Combat_ChaseSpeed_900` |
| Raptor | `Combat_Role_PackHunter` | `Combat_AttackRange_200` | `Combat_ChaseSpeed_1100` |
| Triceratops | `Combat_Role_DefensiveCharger` | `Combat_AttackRange_300` | `Combat_ChaseSpeed_700` |
| Brachiosaurus | `Combat_Role_Passive_FleeOnly` | `Combat_AttackRange_0` | `Combat_ChaseSpeed_0` |

All tagged actors also received a `Combat_TacticalAI_v1` marker tag so future cycles can detect this work already happened and avoid re-tagging (idempotency guard, same principle as the label-dedup rule but applied to tags instead of new actors).

## Verification
A 4th `ue5_execute` call read back tags on all dinosaur actors within 3500 units of the hub and logged `VERIFY <label> -> [Combat_* tags]` for each, confirming the write succeeded. Also confirmed `/Script/AIModule.BehaviorTree` and `/Script/AIModule.AIController` are loadable engine classes — these are the correct existing UE5 classes to wire actual Behavior Trees onto in a future cycle (NOT custom C++ AI classes, per engine-first rule).

## Combat Audio & Concept Art (generated server-side, storage upload failed)
- 2x `text_to_speech` combat vocalizations generated successfully server-side (Raptor pack alert bark sequence, T-Rex territorial pre-charge roar) but hit the **known cross-agent Supabase `403 Invalid Compact JWS`** storage bug — same failure as logged in prior cycle memory (PROD_CYCLE_AUTO_20260712_004). Raw base64 MP3 payloads were returned by the TTS API itself (generation succeeded), only the Supabase upload step failed. This is an infrastructure bug outside this agent's control.
- 1x `generate_image` (HD, 1792x1024) — Velociraptor pack ambush combat concept art — same storage upload failure (image generated by gpt-image-1 server-side, upload to Supabase rejected).

## Next Agent Focus (#13 Crowd & Traffic Simulation)
1. The `Combat_TacticalAI_v1` tag marker exists — check for it before re-tagging the same actors.
2. Do NOT create more `CombatZone_*`/`BehaviorTag_*` floating actors — the flood must stop being fed.
3. `/Script/AIModule.BehaviorTree` + `/Script/AIModule.AIController` confirmed loadable — a future cycle should build actual BT assets in the Editor (via Python `unreal.AIModule` asset creation, NOT custom C++) referencing these Combat_ tags as Blackboard keys.
4. Escalate the Supabase `403 Invalid Compact JWS` storage bug to Studio Director (#01) — it has now blocked audio/image delivery across at least 2 agents/cycles independently and needs an infra-level fix (JWT/token refresh on the storage service), not a per-agent workaround.
