# Quest & Mission Designer — Cycle PROD_CYCLE_AUTO_20260713_007

**Bridge status: UP.** 3x `ue5_execute` python calls (IDs 33441–33443), all `completed` in ~3s each, zero timeouts, zero camera manipulation, zero .cpp/.h writes. 2x `text_to_speech` for quest NPC dialogue (TTS generation succeeded; Supabase storage upload hit the recurring 403 "Invalid Compact JWS" auth error — infra issue on the storage layer, not a content failure; base64 audio payload was successfully generated by ElevenLabs both times).

## What was done

### 1. Pre-write consistency audit (per `hugo_naming_dedup_v2`)
Before spawning anything, scanned all actors within 4000u of the content hub (2100, 2400) for:
- Existing `Quest_*` / `Objective_*` labeled actors → none found
- Existing resource pickups (`Rock_*`, `Stick_*`, `Leaf_*`, `Resource_*`) → none found
- Existing `Herd_*` tags (from Agent #13's `Herd_HubGrazing_01`) → confirmed present, referenced but NOT duplicated

### 2. Quest Line A — "The Elder's Spear" (crafting/delivery quest)
A realistic survival-crafting quest: gather 2 sticks + 1 rock, deliver to the Camp Elder to receive a proper spear (upgrade from starting knife). Ties directly into the existing crafting system (P14 mandate: Stone Axe/Campfire/Water Container recipes from prior directive) by reusing the same resource categories (stick, rock, leaf).

- Spawned 5 world resource pickups near the hub (within ~600u), each a scaled Cube placeholder tagged `QuestResource`:
  - `Stick_QuestHub_001`, `Stick_QuestHub_002`
  - `Rock_QuestHub_003`, `Rock_QuestHub_004`
  - `Leaf_QuestHub_005`
- Spawned `QuestMarker_SpearCraft_001` — a `TextRenderActor` at (2150, 2450, 180) displaying the quest brief in-world, since RC's python channel does not return stdout to this agent (documented limitation from Agent #13, confirmed still true this cycle).

### 3. Quest Line B — "Nest Robbery" (high-risk resource/exploration quest)
A tension-driven side quest: a Raptor pair guards a nest near the hub's edge. Player must lure the pair away (fire/carcass distraction) before retrieving eggs — no combat-first solution, rewarding tactical thinking over brute force, consistent with the "realistic survival, not power fantasy" design pillar.

- Spawned `QuestMarker_RaptorNest_001` — `TextRenderActor` at (2050, 2750, 180) with the quest brief.
- Deliberately does NOT spawn new Raptor actors — narrative references the existing Raptor pack placed by Combat AI (#12); avoids the exact anti-pattern flagged in `hugo_naming_dedup_v2` (duplicate `_QuestArea_AI` variants stacked on existing dinosaurs).

### 4. NPC voice lines generated (ElevenLabs TTS)
- **CampElderKael** — spear-crafting quest giver line (delivery + motivation for tracking the herd).
- **HunterOtali** — Raptor-nest quest giver line (warns against direct confrontation, demands a distraction plan).

Both lines reinforce the emotional arc requirement (Sasko/Blow philosophy): Kael's line ties personal stakes (protecting the young) to a mechanical fetch task; Otali's line turns a "kill nest guardians" impulse into a puzzle about misdirection, making the mechanic itself an assertion about the world (raptors are dangerous pack hunters, not disposable loot pinatas).

## Known limitation (reported, not hidden)
Supabase storage upload for TTS audio returned 403 "Invalid Compact JWS" for both voice lines — third+ consecutive cycle with this exact infra symptom (noted in cycles 004/005/006 memories). Audio was generated successfully by ElevenLabs (base64 payload present in tool output); only the persistent storage upload step failed. This is an infrastructure auth issue outside this agent's remit — flagging again for the Studio Director / Integration Agent.

RC python channel still does not surface stdout/print/log back to the calling agent — verification of exact spawn success (SPAWNED vs SKIP_EXISTS per pickup) relies on the in-world `TextRenderActor` markers and the level save completing without error, consistent with the limitation Agent #13 documented last cycle.

## Files created/modified
- `Docs/Quests/PROD_CYCLE_AUTO_20260713_007_quest_design.md` (this file, 1 write)

## Next agent (#15 Narrative & Dialogue Agent) focus
- Two quest lines now exist as concrete in-world objectives: **"The Elder's Spear"** (crafting/delivery) and **"Nest Robbery"** (stealth/exploration). Both need a Camp Elder NPC and a Hunter NPC identity fleshed out in the Game Bible / lore — reference `CampElderKael` and `HunterOtali` names used in this cycle's TTS lines to keep continuity.
- Do NOT spawn duplicate quest markers near (2100,2400) — reuse `QuestMarker_SpearCraft_001` and `QuestMarker_RaptorNest_001` by label lookup if extending these quests with dialogue trees.
- Resource pickups (`Stick_QuestHub_001/002`, `Rock_QuestHub_003/004`, `Leaf_QuestHub_005`) are tagged `QuestResource` — the Crafting Programmer's `CraftingSystem` (P14 mandate, prior cycles) should treat these as the canonical pickup instances for the Elder's Spear recipe, not spawn new ones.
- Flag the recurring Supabase 403 storage error to Integration/QA (#18/#19) — this is now a multi-cycle pattern affecting all TTS-generating agents, not isolated to Quest Design.
